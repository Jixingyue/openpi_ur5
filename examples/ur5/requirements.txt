# Minimal deps for examples/ur5 real-robot client (see README).
# Also install openpi-client from the repo root:
#   uv pip install -e packages/openpi-client
tyro>=0.9
numpy>=1.24
opencv-python-headless>=4.8
einops>=0.8
pyrealsense2>=2.55
ur-rtde>=1.5
pymodbus>=3.6.0
